spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/spring_boot_security
    username: postgres
    password: 12345678pP
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
server:
  port: 8080
testing:
  app:
    expiration: 86400000
    secret: mySuperDifficultSecret